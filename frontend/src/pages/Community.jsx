import { PageContainer } from '../components/layout';
import { EmptyState } from '../components/ui';

export default function Community() {
  return (
    <PageContainer title="Community" subtitle="Feed, Creator Hub, and Community Pins.">
      <EmptyState title="Community module placeholder" description="Feature-specific UI will live under features/community." />
    </PageContainer>
  );
}
